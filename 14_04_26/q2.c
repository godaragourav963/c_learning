//prnt the list of element which is proving the product of digit is more than 50
#include<stdio.h>
int main(){
    int a=5,product=1;
    // scanf("%d",&a);
    // int arr[a];
    // for(int i=0;i<a;i++){
    //     scanf("%d",&arr[i]);
    // }
    int arr[5] = {11,45,87,94,99};
    int *p=arr;
    for(int i=0;i<a;i++){
        // while(*(p+i)>0){
        // int digit = *(p+i)%10;
        // product *= digit;
        // *(p+i) /= 10;
        // }
        //printf("%d ",product);
        // if(product>50){
        //     printf("%d ",*(p+i));
        // }
        printf("%d ",*(p+i));
        product=1;
    }
    
}